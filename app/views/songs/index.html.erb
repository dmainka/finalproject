<% provide(:title, 'All Songs') %>

<%= render partial: 'layouts/search', locals:{path: songs_path} %>

<% session[:last_product_page] = request.env['HTTP_REFERER'] || products_path %>

<h1>All Songs</h1>

<%= will_paginate %>

<ul class="songs">
  <%= render @songs %>
</ul>

<%= will_paginate %>
