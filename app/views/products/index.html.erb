<% provide(:title, 'All Products') %>

<%= render partial: 'layouts/search', locals:{path: products_path} %>


<h1>All Products</h1>

<%= will_paginate %>

<ul class="products">
  <%= render @products %>
</ul>

<%= will_paginate %>
